package handlers

import "github.com/labstack/echo/v4"

func (h *Handlers) GetVideoURL(e echo.Context) error {
// 	const getVideoUrl = async (request, response) => {
//   const { courseId, storageKey } = request.body;

//   try {
//     const key = getVideoKey(courseId, storageKey);
//     const url = await getCloudfrontURL(key);

//     response.status(200).json({ url });
//   } catch (error) {
//     handleError(response, error, errorMessagesMap.get_video_url_error);
//   }
// };
}