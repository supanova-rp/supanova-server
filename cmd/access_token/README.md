# Get access token Firebase tool

Used to fetch an access token from Firebase.

### Prerequisites

Firebase Web API Key from the Firebase dashboard.

### Usage

From the terminal, run: 

`go run cmd/access_token/main.go -api-key=<Firebase_web_API_key> -email=<user_email_address> -password=<user_password>`
